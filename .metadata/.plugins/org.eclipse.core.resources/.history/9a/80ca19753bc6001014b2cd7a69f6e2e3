package sam;

import com.formdev.flatlaf.FlatDarkLaf;

import javax.swing.*;
import java.awt.*;
import java.io.*;
import java.net.*;
import java.nio.charset.StandardCharsets;
import java.util.concurrent.ConcurrentHashMap;

public class Server {

    private static final int UDP_RECV_PORT = 5000;
    private static final int UDP_BROADCAST_PORT = 5001;
    private static final int TCP_FILE_PORT = 6000;
    private static final String BROADCAST_IP = "255.255.255.255";

    private JFrame frame;
    private JPanel panelMessages;
    private JScrollPane scrollPane;
    private JTextField input;
    private JTextArea onlineArea;
    private DatagramSocket udpRecvSocket;
    private final ConcurrentHashMap<String, String> online = new ConcurrentHashMap<>();
    private final File uploadsDir = new File("uploads");

    public Server() {
        if (!uploadsDir.exists()) uploadsDir.mkdirs();

        try {
            UIManager.setLookAndFeel(new FlatDarkLaf());
        } catch (Exception ex) {
            ex.printStackTrace();
        }

        createUI();
        startUdpReceiver();
        startTcpFileServer();
    }

    private void createUI() {
        // Tương tự Client, chỉ đổi tiêu đề và màu nhấn
        frame = new JFrame("SERVER - Messenger");
        frame.setSize(1100, 720);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.getContentPane().setBackground(new Color(30, 30, 36));

        // Header, Sidebar, Input... copy từ Client, chỉ đổi màu nhấn thành xanh lá
        // (Để ngắn, bạn copy toàn bộ phần createUI() từ Client, chỉ đổi màu bubble Server thành Color(0, 200, 100))

        // ... (code UI giống Client, chỉ đổi tên và màu)

        // Bạn chỉ cần thay thế toàn bộ createUI() và addMessageBubble() bằng code từ Client
        // Đổi màu bubble của Server thành: new Color(0, 200, 100)
        // Và tiêu đề thành "SERVER - Messenger"

        // Vì quá dài, bạn cứ copy toàn bộ Client.java rồi đổi tên class thành Server, đổi màu, đổi tiêu đề là xong!

        // Hoặc nếu bạn muốn mình gửi file Server hoàn chỉnh luôn thì reply: "Gửi luôn Server đẹp đi anh ơi" 😄
    }

    // Các hàm UDP, TCP, broadcast, handleFileClient giữ nguyên 100% như cũ

    public static void main(String[] args) {
        SwingUtilities.invokeLater(Server::new);
    }
}